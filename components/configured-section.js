import ConfiguredSectionPlaceholder from "./configured-section-placeholder";

const ConfiguredSection = ({ ghiChu }) => {
  return <ConfiguredSectionPlaceholder ghiChu={ghiChu} />;
};

export default ConfiguredSection;
